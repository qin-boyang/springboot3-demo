/**
 * Shared events module containing events that can be published and consumed
 * by different modules to achieve loose coupling.
 */
@ApplicationModule(
        displayName = "shared"
)
package com.example.springboot_demo.shared;

import org.springframework.modulith.ApplicationModule;
